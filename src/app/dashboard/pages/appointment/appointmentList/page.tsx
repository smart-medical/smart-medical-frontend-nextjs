import { AppointmentListTable } from '@/app/dashboard/tables/AppointmentListTable'

export default function AppointmentListPage() {
  return (
    <div>
        <h1 className="text-2xl font-bold ">Appointment List</h1>
        <AppointmentListTable />
    </div>
  )
}
